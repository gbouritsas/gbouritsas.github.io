baseURL = "https://gbouritsas.github.io/gbouritsas-site/"
languageCode = "en-us"
title = "Giorgos Bouritsas"

[taxonomies]
  # Disabled — single-page site doesn't use tags or categories

[markup.goldmark.renderer]
  unsafe = true
